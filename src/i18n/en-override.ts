/**
 * Tenant-specific English overrides.
 *
 * Keep the shared dictionary in `en.ts`. Add only the keys this tenant
 * needs to change or introduce. Nested keys are deep-merged
 * (e.g. `routeLabel.scientificResults`).
 */
export default {
};
