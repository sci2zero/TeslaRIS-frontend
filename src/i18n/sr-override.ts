/**
 * Tenant-specific Serbian (Latin) overrides.
 *
 * Keep the shared dictionary in `sr.ts`. Add only the keys this tenant
 * needs to change or introduce. Nested keys are deep-merged
 * (e.g. `routeLabel.scientificResults`).
 */
export default {

};
