/**
 * Tenant-specific Serbian (Cyrillic) overrides.
 *
 * Keep script-level corrections in `sr-cyr.ts`. Add only the keys this
 * tenant needs to change or introduce. Nested keys are deep-merged
 * (e.g. `routeLabel.scientificResults`).
 *
 * Applied last: after `sr` + `sr-override` are transliterated and
 * merged with `sr-cyr.ts`.
 */
export default {
};
